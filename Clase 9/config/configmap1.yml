apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  # Pares clave-valor simples
  APP_ENV: "production"
  APP_PORT: "8080"
  LOG_LEVEL: "info"
  DB_HOST: "postgres-svc.default.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "mi_base_de_datos"