[package]
name    = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
tiny_http   = "0.12"   # Servidor HTTP minimalista
serde       = { version = "1", features = ["derive"] }  # Macros Deserialize / Serialize
serde_json  = "1"      # Leer y escribir JSON